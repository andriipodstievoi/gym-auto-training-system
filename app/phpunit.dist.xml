<?xml version="1.0" encoding="UTF-8"?>

<!-- https://phpunit.readthedocs.io/en/latest/configuration.html -->
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         colors="true"
         failOnDeprecation="true"
         failOnNotice="true"
         failOnWarning="true"
         bootstrap="tests/bootstrap.php"
         cacheDirectory=".phpunit.cache"
>
    <php>
        <ini name="display_errors" value="1" />
        <ini name="error_reporting" value="-1" />
        <server name="APP_ENV" value="test" force="true" />
        <server name="SHELL_VERBOSITY" value="-1" />
    </php>

    <testsuites>
        <testsuite name="Project Test Suite">
            <directory>tests</directory>
        </testsuite>
    </testsuites>

    <source ignoreSuppressionOfDeprecations="true"
            ignoreIndirectDeprecations="true"
            restrictNotices="true"
            restrictWarnings="true"
    >
        <include>
            <directory>src</directory>
        </include>

        <exclude>
            <!-- Framework plumbing and seed data, not logic this project owns. -->
            <file>src/Kernel.php</file>
            <directory>src/DataFixtures</directory>
        </exclude>

        <deprecationTrigger>
            <method>Doctrine\Deprecations\Deprecation::trigger</method>
            <method>Doctrine\Deprecations\Deprecation::delegateTriggerToBackend</method>
            <function>trigger_deprecation</function>
        </deprecationTrigger>
    </source>

    <!--
       Coverage was off through M0 to M6 so the suite stayed fast while the
       shape of the app was still moving. It is on in CI now, so a drop shows
       up in the diff rather than months later.

       The reports are asked for on the command line rather than configured
       here on purpose: naming a report element makes a coverage driver
       mandatory, and a plain "bin/phpunit" on a machine without xdebug then
       exits with "No tests executed" instead of running the suite. What the
       source element above includes and excludes still governs what gets
       measured. See the CI workflow for the exact invocation.
    -->

    <extensions>
    </extensions>
</phpunit>
