import './styles/app.css';

import Alpine from 'alpinejs';

// Alpine drives the small interactive bits: nav, language switcher,
// and later the multi-step training questionnaire.
window.Alpine = Alpine;
Alpine.start();
